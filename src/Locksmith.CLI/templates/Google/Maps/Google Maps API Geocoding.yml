title: 'Google Maps API Geocoding'
url: https://maps.googleapis.com/maps
details: Key restrictions are set per service. When testing the key, if the key is restricted/inactive on one service try it with another. Cost $5
commands:
- tool: curl
  command: 'https://maps.googleapis.com/maps/api/geocode/json?latlng=40,30&key=KEY_HERE'
  parameters:
  - description: KEY_HERE
    key: KEY_HERE
    default_value:
regex: