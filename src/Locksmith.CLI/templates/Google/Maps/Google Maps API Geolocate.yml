title: 'Google Maps API Geolocate'
url: https://maps.googleapis.com/maps
details: Key restrictions are set per service. When testing the key, if the key is restricted/inactive on one service try it with another. Cost Varies
commands:
- tool: curl
  command: 'https://www.googleapis.com/geolocation/v1/geolocate?key=KEY_HERE'
  parameters:
  - description: KEY_HERE
    key: KEY_HERE
    default_value:
regex: