title: 'Google Maps API Static Maps'
url: https://maps.googleapis.com/maps
details: Key restrictions are set per service. When testing the key, if the key is restricted/inactive on one service try it with another. Cost $2
commands:
- tool: curl
  command: 'https://maps.googleapis.com/maps/api/staticmap?center=45%2C10&zoom=7&size=400x400&key=KEY_HERE'
  parameters:
  - description: KEY_HERE
    key: KEY_HERE
    default_value:
regex: